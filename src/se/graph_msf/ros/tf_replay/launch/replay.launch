<?xml version="1.0" encoding="UTF-8"?> 
<launch>
    <!-- Arguments -->
    <arg name="use_sim_time" value="true"/>
    <arg name="dumped_param_file" default="$(find anymal_estimator_graph)/config/dumped_params_d/2023-01-13-16-27-14_anymal-d020-lpc_mission.yaml"/>

    <!-- Set use sim time -->
    <param name="use_sim_time"            value="$(arg use_sim_time)"/>

    <!-- Load dumped parameters, mainly for description -->
    <rosparam command="load" file="$(arg dumped_param_file)" />

    <!-- TF from odometry -->
    <node type="tf_replay_node" name="tf_replay_node" pkg="tf_replay" output="screen" />

    <!-- RVIZ for Visualization -->
    <node type="rviz" name="rviz" pkg="rviz" args="-d $(find tf_replay)/rviz/replay.rviz" />

</launch>
